# memodb-io/Acontext

Repo: https://github.com/memodb-io/Acontext
License: Apache-2.0
What we took: nothing — this is a self-hosted memory service (Docker + LLM
API key), not prompt content. Registered as a workflow pointer only:
`workflows/skill-memory/manifest.yaml`.
Status: not absorbed (pointer). Its "memory as editable skill files"
philosophy is worth reading even if you don't run the service — it's close
to the design of this registry itself.
